spring.application.name=${SPRING_APPLICATION_NAME:ftgo-order-service}

logging.level.org.hibernate.SQL=${LOGGING_LEVEL_HIBERNATE_SQL:INFO}
spring.jpa.generate-ddl=${SPRING_JPA_GENERATE_DDL:true}
stubrunner.stream.enabled=${STUBRUNNER_STREAM_ENABLED:false}
stubrunner.integration.enabled=${STUBRUNNER_INTEGRATION_ENABLED:false}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost/eventuate?useSSL=false}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:mysqluser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:mysqlpw}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}

server.port=${SERVER_PORT:8080}
management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS:health,info,metrics}
